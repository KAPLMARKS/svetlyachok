# Базовый URL FastAPI-бэкенда. По умолчанию `/api` — попадает в dev-proxy
# Vite, который проксирует на http://localhost:8000.
#
# Для прямого подключения (без proxy) задайте полный URL:
#   VITE_API_BASE_URL=http://localhost:8000/api
VITE_API_BASE_URL=/api
